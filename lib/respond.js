'use strict';

/*
	This is how we actually respond to a request, given a normalized response.
	TODO: actually respond
 */

module.exports = (res, { code, message, headers, body, trailers }, onError) => {
	
};
